# Uncomment this line to define a global platform for your project
platform :ios, '9.3'

# Google Sign In Pod
pod 'Google/SignIn'

target 'ios-dev' do
  # Comment this line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for ios-dev

  target 'ios-devTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'ios-devUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
